### todo ###

bugs

-x Sides is wrong - it's clearly not generating all the sides properly
  -x turns out issue was the parent node value being air

-x redo ChunkTextured mesh building system
-x new game is missing a ui
- windows build in zelder, make a bsh/windows.sh
- controller hotkeys broken for steamdeck
-x first navigator selection missing for gamepad navigation mode
- fix end game fade out, really bad atm
- fix memory growth when returning to main menu and returning - realm not destroying right?
- fix music file not importing anymore
- sometimes raycasting not working on chunk - itll just shoot past first block - scale issue?
- chunk / parts of them sometimes missing render now
- we need to slice mesh properly for voxel faces

tools (this will help fix bugs)

- press 'f' while raycasting chunk to select it as debug target
  - this will show the 'chunk ui' which shows all chunk info
  - itll have a close button on it
  - Stats
    - Verts
    - States?
    - Idk