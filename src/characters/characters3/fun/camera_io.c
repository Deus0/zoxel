void load_camera_e(ecs *world, entity realm, entity e) {
    /*zox_geter(realm, SaveGamePath, path);
    load2_camera(path->value, "camera.dat", &camera_save);
    zox_set(e, Euler, { camera_save.euler });
    zox_set(e, Rotation3D, { quaternion_from_euler(camera_save.euler) });
    zox_set(e, LocalRotation3D, { camera_save.lrotation });*/
}
