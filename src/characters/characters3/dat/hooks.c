/*typedef struct {
    const entity realm;
    const entity e;
    const entity p;
    // spawn info
    const char *name;
    const byte render_disabled;
    entity health;
    float soul_value;
    // massive links
    ElementLinks *elementLinks;
    const entity *stats;
    byte stats_length;
} spawned_character3D_data;

zox_hook(spawned_character3D, (ecs* world, spawned_character3D_data *data), (world, data))*/