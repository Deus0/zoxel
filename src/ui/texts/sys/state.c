
/*void text_dirty_system(iter* it) {
    zox_sys_world();
    for (int i = 0; i < it->count; i++) {
        zox_sys_e();
        zox_remove(e, TextDirty);
        zox_add(e, Update);
    }
} zoxd_system(text_dirty_system);*/
