// NOTE: Assume Max Depth
/*typedef struct {
    byte level;
    int2 position;
    int distance;
} TerrainSpawnUpdate;

zoxc_queue(TerrainSpawnQueue, TerrainSpawnUpdate, 1);
zoxc_queue_remove(TerrainSpawnQueue, TerrainSpawnUpdate);

void define_terrain_spawn_queue(ecs* world) {
    zoxd_queue(TerrainSpawnQueue);
}
*/