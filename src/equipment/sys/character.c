void spawn_character_equipment(
    ecs *world,
    spawned_character3D_data *data
) {
    if (!data->p) {
        return;
    }

    // test - Spawn Tophat!

    // zox_set_ptr(data->e, EquipLinks, equips);
}
