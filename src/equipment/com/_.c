zox_tag(MenuEquipment);
zox_tag(EquipItem);
zoxc_entities(EquipLinks)

void define_components_equipment(ecs* world) {
    zoxd_tag(MenuEquipment);
    zoxd_tag(EquipItem);
    zoxd_entities(EquipLinks)
}
