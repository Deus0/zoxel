zox_tag(Region);
zoxc_int3(RegionPosition);
zoxc_entity(RegionLink);

void zox_define_components_regions(ecs* world) {
    zoxd_tag(Region);
    zoxd_int3(RegionPosition);
    zoxd_entity(RegionLink);
}
