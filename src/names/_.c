#ifndef zoxm_names
#define zoxm_names

#include "com/_.c"
#include "sys/_.c"

zox_begin_module(Names) {
    zox_define_components_names(world);
    zox_define_systems_names(world);
} zox_end_module(Names);

#endif
